# Azure App Registration (Rotate for each campaign)
AZURE_CLIENT_ID=your-app-client-id-here
AZURE_CLIENT_SECRET=your-app-client-secret-here
AZURE_TENANT_ID=NETORGFT4679501.onmicrosoft.com

# Auto-Gen Tor IP (Run once: docker run --rm alpine/wget -qO- ifconfig.me via Tor)
TOR_IP=your-obfuscated-tor-ip-here

# Optional: Redis/Proxy overrides (defaults fine)
REDIS_URL=redis://redis-sessions:6379
